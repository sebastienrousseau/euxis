#pragma once

#include <cstddef>
#include <vector>

namespace euxis::crypto {

/**
 * @brief Simple alias for a buffer of raw bytes.
 */
using ByteVector = std::vector<std::byte>;

} // namespace euxis::crypto
