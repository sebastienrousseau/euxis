{
  "project": "${PROJECT_NAME}",
  "mode": "hierarchical",
  "dispatches": [
    {
      "agent": "${AGENT_ID}",
      "priority": "P0",
      "task": "${TASK_DESCRIPTION}",
      "verify_cmd": "${VERIFY_COMMAND}"
    }
  ]
}
