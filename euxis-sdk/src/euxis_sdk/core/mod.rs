//! Core shared models and error types for traversing MCP.

/// Domain error states returned from the MCP execution stream
pub mod error;

/// Domain types used for message passing cross-boundary
pub mod types;
