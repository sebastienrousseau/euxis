# Support & Issue Resolution

If you encounter architectural instability natively executing specific Extism plugins, or experience gateway initialization telemetry faults natively that fall beyond the boundaries of the `Common Errors` configuration manual natively, leverage the Euxis core infrastructure reporting pipelines intrinsically.

## Preparing the Telemetry Dump

Before dispatching an explicit Support ticket tracking incident to the Euxis GitHub routing repository natively, definitively compile an execution telemetry trace accurately.

Euxis builds explicit telemetry snapshot compression arrays autonomously using generic support generation routes. 

```bash
euxis support generate
```

This autonomously creates a sterile `.tar.gz` diagnostic envelope specifically omitting API payload keys internally while mapping the explicit gateway version parameters, host OS system structures natively, and the exact WebAssembly memory boundary constraints directly inside the archive structure securely.

## Submitting an Issue

Navigate entirely to the Euxis open-source core infrastructure repository immediately, and explicitly file a standard `Bug Report`.

1. **Upload the Dump:** Attach the explicitly generated support tarball natively.
2. **Contextualize the Fault:** Explain implicitly exactly what generic invocation explicitly triggered the gateway exception natively.
3. **Include Minimum Reproduction Syntax:** Attempt to isolate the fault recursively so maintainers can execute your exact generic CLI intent flawlessly natively upon initialization.

## Community & Forums

Do not utilize the Github Bug Repository identically for generic architectural usage clarification intents natively. Instead, migrate generic questions explicitly natively into the standard Discord support forums or explicit Github Discussion threads explicitly intended for usage integration questions natively.
