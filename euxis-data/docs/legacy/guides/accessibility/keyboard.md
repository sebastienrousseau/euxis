# Keyboard Navigation

The Euxis orchestrator Terminal User Interface (TUI) natively eliminates cursor traversal completely. Rely exclusively utilizing structural hotkey bindings natively to traverse the interface panels fluently without abandoning the keyboard array explicitly.

## Standard Binding References

| Keystroke | Operational Directive |
|---|---|
| `Tab` | Shift explicit UI focus between the active Fleet Graph, Telemetry Logger, and Chat Input modules iteratively. |
| `Shift + Tab` | Reverse the logical module focus iteration natively. |
| `/` (Forward Slash) | Instantly capture input focus into the main intent submission field, unconditionally of your current structural hierarchy natively. |
| `Esc` | Drop explicit text input focus and return navigation explicitly to the Fleet Graph natively. |
| `Up / Down Arrows` | Traverse generic telemetry boundaries or active squad task lists exclusively natively. |

## Application Control Bindings

Manage the holistic Gateway daemon directly from the TUI natively using explicit system hooks intrinsically.

| Keystroke | Operational Directive |
|---|---|
| `Ctrl + C` | Trigger graceful termination. Signal all executing agents generically to flush memory buffers and dump telemetry arrays securely natively. |
| `Ctrl + X` | Execute forceful annihilation natively. Drop Wasm arrays instantly and terminate the UI natively without waiting for specific mesh responses. |
| `Ctrl + L` | Clear the visual telemetry buffer cache specifically, purging historical execution routes explicitly from the visible TUI viewport. |
