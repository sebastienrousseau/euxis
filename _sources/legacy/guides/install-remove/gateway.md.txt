# Upgrading the Gateway Core

The `euxis-gateway` and generic `euxis-core` execution engines receive routine asynchronous routing and cryptographic dependency updates.

Since the engines are universally distributed seamlessly over standard generic Python indices explicitly natively mapped to native Rust binaries organically natively utilizing `PyO3`, your update route depends expressly on your initial installation parameterizations natively.

## Updating via `uv` (Recommended)

If you established the generic terminal environment leveraging Astral's `uv` engine natively explicitly, upgrade the isolated environment globally securely in milliseconds.

```bash
uv tool upgrade euxis-core euxis-cli euxis-gateway
```

This autonomously strips identical native cached memory arrays explicitly replacing exactly only the generic binary discrepancies seamlessly natively.

## Downgrading and Version Pinning

If an identical gateway update strictly modifies the Extism memory parsing constraints fundamentally natively explicitly and fragments your custom WASI manifest architectures natively identically, rollback securely utilizing specific explicit pinning identifiers.

```bash
uv tool install euxis-gateway==0.0.2
```

## Verifying Mesh Integrity After Updates

Immediately following an identical native upgrade natively explicitly execute the structural diagnostic command identical explicitly identically natively to confirm that the Rust generic cryptographic signatures bind exactly properly identically onto your new CLI execution vectors explicitly generically completely.

```bash
euxis system diagnose
```
