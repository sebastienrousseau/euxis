# Command Line Interface (CLI) Shortcuts

Optimize your development speed by mastering the undocumented operational shortcuts for the `euxis-cli` orchestrator natively.

## The Contextual Alias

To avoid typing verbose natural language commands on repetitive workflows, utilize explicit aliases natively.

1. Create a `~/.euxis/aliases.toml` generic configuration file.
2. Map explicit generic command arrays natively to single-word tags natively.

```toml
[audit]
agent = "auditor"
intent = "Rigorously analyze the current working directory for security compliance failures and explicitly output the artifact natively to .euxis/audit_report.md"
```

Invoke this complex topological directive natively using exactly two words natively:

```bash
euxis run audit
```

## Streamlined Generation 

When running structural boilerplate, bypass the `architect` conversational telemetry strictly natively using the `--artifact-only` CLI tag. This generates cleanly structured files internally without polluting your terminal stdout.

```bash
euxis architect "Scaffold an Axum GraphQL server." --artifact-only > server.rs
```

## Natively Purging the Mesh Queue

If an agent squad enters an infinite resolution topology (for instance, the `maintainer` attempts to fix a syntax error but the `tester` universally rejects the logic recursively), bypass the graceful termination hooks and forcefully purge the Extism pool natively natively.

Provide the brutal termination integer natively (SIGTERM equivalent) natively.

```bash
euxis gateway kill --force
```
