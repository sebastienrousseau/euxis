# Core Concepts

The Euxis core-concept reference lives at [`essentials/core-concepts/`](essentials/core-concepts/). This page is kept as a stable redirect target so older bookmarks and inbound links keep resolving to a single canonical location.

The destination covers every recurring abstraction the system relies on — agents, squads, combos, playbooks, memory, templates, provider routing, agent selection, coordination choice, codex, branding, and manifests — each in its own dedicated page.
