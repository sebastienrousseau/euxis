INPUT: Add tests for parse_date(s) that accepts YYYY-MM-DD and returns a date or raises on invalid input.
EXPECTED_OUTPUT: Must include valid case, invalid format, and at least one edge case (leap year or out-of-range date).
