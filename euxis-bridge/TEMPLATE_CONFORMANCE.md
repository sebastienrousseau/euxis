# Template Conformance

This package follows the Euxis package template:

- `src/euxis_bridge/core`: platform-agnostic bridge logic
- `src/euxis_bridge/platform`: macOS/Linux/WSL adapters
- `tests/unit`: deterministic core tests
- `tests/platform`: platform adapter contract tests
